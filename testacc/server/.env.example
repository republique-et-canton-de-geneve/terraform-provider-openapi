# No required variables — the server runs with SQLite and no authentication by default.
# Copy this file to .env and uncomment variables to override.

# SECRET_KEY=change-me-in-production
# DJANGO_DEBUG=false
